# Phase 3: SMS + Agent Interface + CLI - Context

**Gathered:** 2026-03-24
**Status:** Ready for planning

<domain>
## Phase Boundary

An LLM can use Holler as a tool — initiating calls, sending and receiving SMS, and completing the full workflow in four CLI commands from a clean install. Delivers: SMPP messaging (send, receive, delivery status), tool-use protocol exposing `call`, `sms`, `hangup`, `transfer` as LLM tool invocations, LLM-agnostic adapter layer, built-in agent loop for `holler call`, and CLI onboarding (`holler init`, `holler trunk add`, `holler call`).

</domain>

<decisions>
## Implementation Decisions

### Tool-use protocol
- **D-01:** Tool definitions use OpenAI function calling JSON Schema format as the canonical representation. Each tool (`call`, `sms`, `hangup`, `transfer`) has typed parameters and structured JSON return values. Python-defined with auto-generated JSON Schema export.
- **D-02:** Compliance blocks return structured error in tool result (`{"status": "blocked", "reason": "..."}`) rather than raising exceptions. The LLM sees the block reason and can inform the human or adjust.
- **D-03:** Transfer tool issues `uuid_transfer` ESL command to redirect the call to a new destination. Destination goes through compliance gateway first. Attended transfer is v2.
- **D-04:** Adapter pattern for multi-LLM support. Tool definitions stored in OpenAI format (canonical). Thin adapters translate to/from provider-specific format (Anthropic tool_use, etc.) in the LLM client layer, not the tool layer.

### SMPP integration
- **D-05:** Use aiosmpplib (async-native) as SMPP client. Fits established asyncio pattern. Supports message segmentation reassembly and delivery receipt correlation.
- **D-06:** Persistent SMPP connection with auto-reconnect. Single bind to SMSC initialized at startup alongside Redis and ESL connections.
- **D-07:** SMS goes through the compliance gateway — TCPA applies to SMS. ComplianceModule gets SMS-aware checking (channel parameter or dedicated method). Same "no bypass" structural guarantee as voice.
- **D-08:** Delivery receipts handled via SMPP delivery receipt PDU callbacks. Receipts update a delivery status store (Redis or in-memory dict keyed by message ID). Status returned to agent as tool response.

### CLI and onboarding
- **D-09:** CLI built with Click framework. `@click.group()` for `holler` command with `init`, `trunk`, `call` subcommands. Entry point registered in pyproject.toml `[project.scripts]`.
- **D-10:** `holler init` checks GPU availability, downloads faster-whisper + Kokoro ONNX models (with progress bars), generates `.holler.env` config file with defaults, starts Docker Compose services (FreeSWITCH + Redis).
- **D-11:** Config file `.holler.env` generated by `holler init`. `HollerConfig.from_env()` reads this file first, then environment variables override. Preserves existing from_env() pattern while adding file-based persistence.
- **D-12:** `holler call +14155551234` uses default voice assistant prompt. `--agent "..."` flag overrides system prompt. Simple, discoverable.

### Agent loop
- **D-13:** Holler provides a built-in agent loop. Loop: LLM receives transcript → generates response (possibly with tool calls) → tool calls execute via Holler → results fed back → LLM continues. External systems can also use Holler as a library and run their own loop.
- **D-14:** VoicePipeline extended to support tool-calling LLM responses. When LLM returns a tool call instead of text, pipeline coordinator executes the tool, feeds result back to LLM, and continues. Tool-call interception is transparent to the voice pipeline.
- **D-15:** `holler trunk add` configures SIP trunk credentials (host, username, password) and writes them to `.holler.env`. Interactive prompts for required fields, with `--host`, `--user`, `--pass` flags for non-interactive use.

### Claude's Discretion
- SMPP connection configuration details (bind type, system_id, encoding)
- Exact JSON Schema structure for each tool definition
- Model download source URLs and caching strategy
- Docker Compose service startup/health check logic in `holler init`
- Delivery status store implementation (Redis vs in-memory)
- Error handling and retry strategy for SMPP connection drops
- Click command help text and output formatting
- Agent loop iteration limits and timeout handling
- SMS message segmentation for long messages
- Multi-modal session linking (SMS → voice escalation) implementation details

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Project context
- `.planning/PROJECT.md` — Core value, constraints (language, latency, no vendor accounts, 4-command onboarding)
- `.planning/REQUIREMENTS.md` — SMS-01..03, AGENT-01..06 requirement definitions
- `.planning/ROADMAP.md` — Phase 3 goal, success criteria, dependency on Phase 2

### Research
- `.planning/research/STACK.md` — Technology versions: aiosmpplib, Click, library compatibility matrix
- `.planning/research/ARCHITECTURE.md` — Component boundaries, data flow, integration patterns
- `.planning/research/PITFALLS.md` — Domain pitfalls including SMPP gotchas, CLI packaging

### Phase 1 implementation (voice pipeline to extend)
- `holler/core/voice/llm.py` — LLMClient with OpenAI-compatible streaming. Extend for tool calling.
- `holler/core/voice/pipeline.py` — VoicePipeline/VoiceSession. Extend coordinator for tool-call interception.
- `holler/core/voice/audio_bridge.py` — AudioBridge WebSocket server. Integration point for agent loop.

### Phase 2 implementation (compliance + telecom to integrate)
- `holler/core/compliance/gateway.py` — ComplianceGateway.originate_checked(), ComplianceModule ABC. Extend for SMS channel.
- `holler/core/telecom/session.py` — TelecomSession. Extend for SMS sessions.
- `holler/core/telecom/pool.py` — NumberPool. SMS may use same pool or separate allocation.
- `holler/core/freeswitch/esl.py` — FreeSwitchESL. Add uuid_transfer for transfer tool.
- `holler/main.py` — Current entry point. Will be refactored alongside CLI integration.
- `holler/config.py` — HollerConfig.from_env(). Extend with SMS config, add .holler.env file loading.

### Package configuration
- `pyproject.toml` — Add Click dependency, aiosmpplib dependency, `[project.scripts]` entry point.

### External concept documents
- `/Users/paul/paul/brains/docs/drafts/2026-03-24-agentic-telecom-concept-brief.html` — Agent interface design, CLI onboarding flow, tool-use protocol concept

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `LLMClient` in `llm.py` — Already uses AsyncOpenAI. Extend `stream_response()` to handle tool-calling responses (function_call in chat completions).
- `ComplianceGateway.originate_checked()` in `gateway.py` — Wrap SMS send in similar pattern for compliance guarantee.
- `ComplianceModule` ABC in `gateway.py` — Extend with SMS checking capability.
- `TelecomSession` in `session.py` — Extend for SMS session tracking (message_id, delivery_status).
- `HollerConfig.from_env()` in `config.py` — Pattern to extend for SMS config and .holler.env file loading.
- `NumberPool` in `pool.py` — DID checkout for SMS sender ID.
- `EventRouter` in `events.py` — Pattern for SMPP event routing (delivery receipts).
- `structlog` — Established logging pattern throughout codebase.

### Established Patterns
- Async throughout (asyncio) — all new components must be async-compatible
- Config-from-environment via dataclass `from_env()` pattern
- Deferred initialization (models loaded on startup via `initialize()`, not in `__init__`)
- Docker Compose for service orchestration (FreeSWITCH, Redis)
- Compliance gateway as mandatory pre-action gate
- TYPE_CHECKING guards for optional imports
- ComplianceResult dataclass for pass/fail with reason and audit fields

### Integration Points
- `llm.py:LLMClient.stream_response()` — Add tool definitions to chat completion requests, handle tool_calls in response
- `gateway.py:ComplianceGateway` — Add `sms_checked()` method parallel to `originate_checked()`
- `config.py:HollerConfig` — Add SMSConfig dataclass, extend `from_env()` with .holler.env loading
- `pyproject.toml` — Add `[project.scripts]` for `holler` CLI entry point
- `main.py` — Refactor to support both CLI invocation and programmatic use

</code_context>

<specifics>
## Specific Ideas

- From concept brief terminal mockup: `holler call +44XXXXXXXXXX --agent "..."` and the call just works — the CLI must feel this effortless
- Four-command onboarding: `pip install holler` → `holler init` → `holler trunk add` → `holler call +1...` — each command prints clear success/failure with next step
- Tool-use protocol treats telecom actions the same way an agent calls an API — `call()`, `sms()`, `hangup()`, `transfer()` are just tool invocations
- "You don't need permission to holler." — the onboarding voice should be warm and direct, not corporate

</specifics>

<deferred>
## Deferred Ideas

- Attended transfer (consult transfer) — v2, blind transfer sufficient for Phase 3
- WebRTC agent-to-agent mesh (MESH-01..03) — v2 requirement, separate from PSTN
- Multi-modal session escalation (SMS → voice mid-conversation) — Phase 3 enables both channels; cross-channel linking is bonus, not requirement
- GSM modem support via gammu — v1 uses SMPP; GSM modem is a deployment option, not core requirement
- Webhook/event callbacks for external systems (MON-03) — v2

None — discussion stayed within phase scope

</deferred>

---

*Phase: 03-sms-agent-interface-cli*
*Context gathered: 2026-03-24*
