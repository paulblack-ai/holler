---
phase: quick
plan: 260324-r7i
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
  - LICENSE
autonomous: true
requirements: []
must_haves:
  truths:
    - "README quick start commands match actual CLI interface"
    - "All links in README resolve to files that exist in the repo"
    - "LICENSE file exists with Apache 2.0 text"
    - "New contributor can find instructions for building a country module"
  artifacts:
    - path: "README.md"
      provides: "Accurate project documentation matching codebase reality"
    - path: "CONTRIBUTING.md"
      provides: "Contributor guide with country module instructions"
    - path: "LICENSE"
      provides: "Apache 2.0 license text"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "markdown link"
    - from: "README.md"
      to: "LICENSE"
      via: "implicit license reference"
---

<objective>
Align README.md with the actual codebase, fix broken links by creating the missing files (CONTRIBUTING.md, LICENSE), and correct inaccurate CLI examples.

Purpose: The README is the front door. Right now it has wrong CLI syntax, references non-existent files, and describes features inaccurately. A developer cloning this repo would hit friction immediately.

Output: Corrected README.md, new CONTRIBUTING.md, new LICENSE file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@holler/cli/commands.py
@holler/countries/_template/module.py
@pyproject.toml
@.env.example
@docker/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README.md to match actual codebase</name>
  <files>README.md</files>
  <action>
Update README.md with these specific corrections:

1. **Quick start section** -- fix CLI commands to match actual interface in holler/cli/commands.py:
   - Change `pip install holler` to local install: `git clone https://github.com/holler-ai/holler && cd holler` then `pip install -e .`
   - Change `holler init` -- this one is correct, keep it
   - Change `holler trunk add --provider voipms --user xxx --pass xxx` to `holler trunk --host sip.example.com --user xxx --pass xxx` (the actual CLI has no `add` subcommand and no `--provider` flag; it uses `--host`, `--user`, `--password`/`--pass`)
   - Keep `holler call +14155551234 --agent "Say hello and ask how their day is going."` (use a US number since the US module is the only one implemented)

2. **Voice pipeline row** in the "How it works" table:
   - Change "faster-whisper + Piper/Kokoro" to "faster-whisper + Kokoro" (Piper/rhasspy is archived per CLAUDE.md "What NOT to Use" section; the codebase uses Kokoro exclusively via kokoro-onnx)

3. **Add a "Prerequisites" section** before Quick start:
   - Python 3.11+ (from pyproject.toml requires-python)
   - Docker (for FreeSWITCH + Redis via docker-compose)
   - A SIP trunk account (the single external dependency)
   - An OpenAI-compatible LLM endpoint (default: Ollama on localhost)

4. **Add a "Project structure" section** after "How it works" showing the actual layout:
   ```
   holler/
     core/
       voice/          # STT, TTS, VAD, pipeline, audio bridge
       freeswitch/     # ESL connection, event handling
       telecom/        # Number pool, sessions, routing, recording
       compliance/     # Gateway, consent DB, DNC, audit log
       agent/          # Tool definitions, executor, LLM adapters
       sms/            # SMPP client, delivery hooks
     countries/
       us/             # TCPA, DNC, time-of-day (implemented)
       _template/      # Scaffold for new country modules
     cli/              # CLI commands (init, trunk, call)
     main.py           # Application entry point
   config/
     freeswitch/       # FreeSWITCH XML config
   docker/             # Docker Compose + Dockerfile
   ```

5. **Fix the country modules section** link:
   - Change `[Build one.](docs/contributing-country-modules.md)` to `[Build one.](CONTRIBUTING.md#country-modules)` (the docs/ dir doesn't exist; point to the CONTRIBUTING.md we're creating in Task 2)

6. **Fix the Contributing section** link:
   - `[CONTRIBUTING.md](CONTRIBUTING.md)` -- this link is fine but the file doesn't exist yet (Task 2 creates it)

7. **Add a "Development" section** near the bottom before Contributing:
   - Show: `pip install -e ".[dev]"` and `pytest` for running tests
   - Mention `.env.example` as config reference

Keep the existing tone (warm, direct, technically honest). Keep "The bridge" section and tagline unchanged -- those are brand voice.
  </action>
  <verify>
    <automated>grep -q "holler trunk --host" README.md && grep -q "Kokoro" README.md && ! grep -q "Piper" README.md && grep -q "CONTRIBUTING.md#country-modules" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README quick start matches actual CLI (holler trunk --host, not trunk add --provider). Voice pipeline says Kokoro not Piper. All internal links point to files that will exist. Prerequisites and project structure sections present.</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md with country module guide</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create CONTRIBUTING.md with the following sections:

1. **Opening** -- brief welcome, mention Apache 2.0, link to LICENSE.

2. **Getting started** -- clone, `pip install -e ".[dev]"`, `pytest` to verify setup.

3. **Country modules** (anchor: `#country-modules`) -- this is the highest-impact contribution area per the README and CLAUDE.md ("The highest-impact contribution is a country module for your jurisdiction"):
   - Explain the ComplianceModule ABC contract: single method `check_outbound(destination, session) -> ComplianceResult`
   - Reference the template at `holler/countries/_template/module.py`
   - Step-by-step: copy template dir, rename class, set E.164 prefix, implement checks (consent, DNC, time-of-day, caller ID, jurisdiction-specific)
   - Point to `holler/countries/us/module.py` as a reference implementation
   - Emphasize: "Start with a markdown file documenting the rules -- the code comes later. Understanding your country's telecom regulations is the hard part."

4. **Code style** -- keep brief: Python 3.11+, type hints, asyncio for I/O, structlog for logging, pytest for tests.

5. **Pull requests** -- standard: fork, branch, test, PR. Keep it short.

Tone: warm, direct, no corporate speak (per CLAUDE.md project character). Match the README voice.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q "country-modules" CONTRIBUTING.md && grep -q "ComplianceModule" CONTRIBUTING.md && grep -q "check_outbound" CONTRIBUTING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CONTRIBUTING.md exists with country-modules anchor. Contains ComplianceModule contract, template reference, step-by-step guide, and reference to US implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add Apache 2.0 LICENSE file</name>
  <files>LICENSE</files>
  <action>
Create the LICENSE file with the standard Apache License, Version 2.0 full text.

Copyright line: "Copyright 2024-2026 Holler Contributors" (project started development recently, use a range that covers the repo history through current year).

Use the canonical Apache 2.0 text from https://www.apache.org/licenses/LICENSE-2.0.txt -- this is a standard license file, no creative decisions needed.
  </action>
  <verify>
    <automated>test -f LICENSE && head -1 LICENSE | grep -qi "apache" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>LICENSE file exists at repo root with Apache 2.0 full text.</done>
</task>

</tasks>

<verification>
All three files exist and cross-reference correctly:
- README.md links to CONTRIBUTING.md (exists) and references Apache 2.0 (LICENSE exists)
- CONTRIBUTING.md links to LICENSE and references codebase paths that exist
- Quick start CLI commands match holler/cli/commands.py actual interface
- No references to non-existent files remain
</verification>

<success_criteria>
- README quick start is copy-pasteable against actual CLI (trunk command, call command)
- Zero broken internal links (CONTRIBUTING.md exists, LICENSE exists)
- Voice pipeline accurately described (Kokoro, not Piper)
- New contributor can follow CONTRIBUTING.md to create a country module from template
- Apache 2.0 LICENSE present at repo root
</success_criteria>

<output>
After completion, create `.planning/quick/260324-r7i-align-the-readme-missing-docs-quick-pass/260324-r7i-SUMMARY.md`
</output>
